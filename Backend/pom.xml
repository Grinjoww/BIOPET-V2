<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.12</version>
        <relativePath/>
    </parent>

    <groupId>com.biopet</groupId>
    <artifactId>biopet-backend</artifactId>
    <version>0.1.0</version>
    <name>BIOPET Backend</name>
    <description>Entrega 1B - JWT + Spring Data JPA</description>

    <properties>
        <java.version>21</java.version>
        <jjwt.version>0.12.6</jjwt.version>
        <springdoc.version>2.5.0</springdoc.version>
        <testcontainers.version>1.21.4</testcontainers.version>
        <!--
            SPIKE FASE 3 - TEMPORAL. Version de xades4j evaluada para la firma
            XAdES-BES del SRI. Si el spike se rechaza, esta propiedad y su
            dependencia (scope test, mas abajo) se eliminan por completo.
        -->
        <xades4j.version>2.4.1</xades4j.version>
    </properties>
<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-bom</artifactId>
            <version>${testcontainers.version}</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
</dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-cache</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <!--
              Fija pgJDBC a 42.7.11 (Spring Boot 3.2.12 gestiona por defecto
              42.6.2). Bug real confirmado en esta fase: 42.6.2 lanza
              "Can't change resolved type for param: 3 from 2278 to 1790"
              en cuanto una conexión reutiliza la llamada a
              fn_reporte_dashboard (parámetro OUT refcursor) más allá del
              prepareThreshold por defecto (5 ejecuciones) — determinista,
              siempre en la 5ª llamada, nunca antes. Corregido en pgJDBC
              agregando `|| paramTypes[index - 1] == Oid.VOID` a la
              condición de sobrescritura permitida en
              SimpleParameterList.setResolvedType (confirmado comparando
              el código fuente 42.6.2 vs 42.7.4/42.7.11). Ver
              ProcedimientosBiopetIntegrationTest.
              reporteDashboard_15InvocacionesConsecutivasEnLaMismaConexion_...
              y DashboardControllerIntegrationTest.
              quinceLlamadasSeguidasAlEndpoint_... (regresión reproducida
              y verificada con esta misma versión revertida).
            -->
            <version>42.7.11</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
    <groupId>org.testcontainers</groupId>
    <artifactId>postgresql</artifactId>
    <scope>test</scope>
    </dependency>
    <dependency>
    <groupId>org.testcontainers</groupId>
    <artifactId>junit-jupiter</artifactId>
    <scope>test</scope>
</dependency>
        <!--
            ================= SPIKE FASE 3 - DEPENDENCIA TEMPORAL =================
            xades4j: unica biblioteca evaluada para producir firmas XAdES-BES
            (ETSI TS 101 903 v1.3.2), el estandar que exige la Ficha Tecnica de
            Comprobantes Electronicos Offline del SRI v2.34, seccion 6.2.

            scope=test A PROPOSITO: el spike vive integramente en
            src/test/java/com/biopet/facturacion/spike/ y NO debe entrar en el jar
            de produccion mientras la decision del spike no este tomada. Arrastra
            transitivamente Apache Santuario (xmlsec), BouncyCastle y Guice; por
            eso NO se declara aqui ninguna dependencia extra de BouncyCastle para
            generar el certificado de prueba (ver dependency:tree del informe).

            Si el spike se RECHAZA: borrar este bloque, la propiedad
            xades4j.version y el paquete spike completo. No queda nada mas que
            deshacer.
            =======================================================================
        -->
        <dependency>
            <groupId>com.googlecode.xades4j</groupId>
            <artifactId>xades4j</artifactId>
            <version>${xades4j.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <!--
                    JWT_SECRET solo para la JVM de "mvn test"/"mvn verify" (nunca se
                    empaqueta en el jar de runtime, ni se agrega a application.yml).
                    Necesario porque security.jwt.secret ya NO tiene fallback en
                    application.yml (P0-1: un secreto conocido en el repo no es un
                    default valido) y varios @SpringBootTest de integracion (los que
                    usan Testcontainers de Postgres sin @ActiveProfiles("test"), p.ej.
                    ProcedimientosBiopetIntegrationTest) cargan el contexto completo
                    -incluyendo JwtService- bajo el perfil por defecto, no bajo
                    application-test.yml. Se reutiliza el mismo secreto de prueba ya
                    versionado en application-test.yml (64 hex = 256 bits), no uno
                    nuevo.
                -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <environmentVariables>
                        <JWT_SECRET>9c8f9a7d6e5b4c3a2d1f0e9c8b7a6d5e4f3a2b1c0d9e8f7a6b5c4d3e2f1a0b9c</JWT_SECRET>
                    </environmentVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.12</version>
                <configuration>
                    <excludes>
                        <exclude>com/biopet/BiopetApplication.class</exclude>
                        <exclude>com/biopet/dto/**</exclude>
                        <exclude>com/biopet/entity/Rol.class</exclude>
                        <exclude>com/biopet/repository/ResumenEspecie.class</exclude>
                        <exclude>com/biopet/config/OpenApiConfig.class</exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.70</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.70</minimum>
                                        </limit>
                                        <limit>
                                            <counter>COMPLEXITY</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.60</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <!--
                    Analisis estatico (SAST) del backend: SpotBugs + Find Security
                    Bugs, con foco en construccion insegura de SQL (equivalente a
                    SQL_NONCONSTANT_STRING_PASSED_TO_EXECUTE y variantes de
                    inyeccion). No se enlaza a ninguna fase del ciclo de vida
                    (sin <executions>) para no alterar el comportamiento de
                    "mvn clean verify" ya validado en fases anteriores: se ejecuta
                    solo cuando se invoca explicitamente, por ejemplo:
                        mvn com.github.spotbugs:spotbugs-maven-plugin:check
                    Ver docs/mediciones/sec/static-analysis/ para el reporte y el
                    resumen de la ultima ejecucion real.
                -->
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <version>4.10.3.0</version>
                <configuration>
                    <effort>Max</effort>
                    <threshold>Low</threshold>
                    <failOnError>true</failOnError>
                    <plugins>
                        <plugin>
                            <groupId>com.h3xstream.findsecbugs</groupId>
                            <artifactId>findsecbugs-plugin</artifactId>
                            <version>1.14.0</version>
                        </plugin>
                    </plugins>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
